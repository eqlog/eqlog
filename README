## Build

Currently phl will always be built with testing, which requires the doctest submodule to be checked out.
Thus if cmake fails make sure to clone all submodules, e.g. with `git submodule update --init`.
