[package]
name = "eqlog-test-eval"
version = "0.1.0"
edition = "2021"

[dependencies]
eqlog-runtime = { path = "../eqlog-runtime" }
maplit = ">= 0"

[build-dependencies]
eqlog = { path = "../eqlog", features = ["rebuild"] }
env_logger = ">= 0"
log = ">= 0"
