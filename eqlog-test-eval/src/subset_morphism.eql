type Carrier;

model Subs {
    pred element(c: Carrier); 
}

func a() -> Subs;
func b() -> Subs;

func a_b() -> Mor(Subs);

rule ab_inclusion_dom {
    if a = a();
    if a_b = a_b();
    then dom(a_b) = a;
}

rule ab_inclusion_cod {
    if b = b();
    if a_b = a_b();
    then cod(a_b) = b;
}
