Support nested models and their morphisms.

Every nested relation prepends its full enclosing-model chain, not
just the innermost parent, so prefix trees can restrict from the
outside in. Membership predicates carry one column per ancestor, and
morphism propagation walks nested models from the inside out.
