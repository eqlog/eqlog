Loosen all dependency version constraints to `>= 0`, pin the rustc
toolchain to 1.95.0, and check in `Cargo.lock` for reproducible builds.
Fix a rand 0.10 compatibility issue (import the new `RngExt` trait so
`random_range` resolves on `StdRng`).
