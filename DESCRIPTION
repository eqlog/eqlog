Rename TypeConflict.cls to TypeConflict.el

Drop the `cls` abbreviation in favour of `el`. Same meaning: the
equivalence-class root ElId where two incompatible TypeIds landed.
Updates both the struct and the translator in algebraize.
