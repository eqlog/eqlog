Add Rust-side syntactic check pass

Move the AST-shape-only compile errors (illegal member type exprs in
signatures, wildcards in then atoms, non-var defined-then targets, and
ill-formed match patterns) off the eqlog-eqlog side into a plain Rust
pass that runs right after parsing.
