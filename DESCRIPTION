Trim Structure::close docstring

The previous docstring was a step-by-step rundown of the inner
fixed-point passes, the deferred-parent-check queue, and the post-loop
canonicalisation — most of it implementation detail that lives in the
code below. Keep just the contract: saturate, canonicalise, return
conflicts, and what `changed` means for an outer fixed point.
