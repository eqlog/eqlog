Always use earliest type-conflict term

Remove the conditional conflict-attribution path and always anchor type
conflicts at the earliest reachable source term. Trim trailing spaces
from rendered source-display lines so updated diagnostics remain
`git diff --check` clean.
