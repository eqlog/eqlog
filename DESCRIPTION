Drop "snapshot" terminology

Purely a naming cleanup: replace every "snapshot" in doc and code
comments with "structure" (or "entry" where it referred to an arena
slot). No type names changed in the previous commit's flat layout.
