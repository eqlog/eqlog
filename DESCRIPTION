Rename fork to clone_structure

RuleStructures::fork becomes RuleStructures::clone_structure (named
to not clash with Clone::clone). Local variables renamed away from
"fork" to match: `next`, `block_start`, `after_scrutinee`,
`case_start`.
