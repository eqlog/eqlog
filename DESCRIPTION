Move type-conflict diagnostics into algebra

Store every concrete type fact per algebra element and report conflicts
only after the structure category reaches its fixed point. Remove the
Eqlog-side conflicting-type diagnostic.

Resolve member and morphism typing against all known concrete types, use
structural type-fact order, and anchor conflicts at the earliest
reachable source term.
