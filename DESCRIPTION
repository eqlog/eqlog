TODO comments for Branch/Match merge-back

Note that Branch's stmt_after and Match's stmt_after should eventually
get a morphism from the intersection of the end structures in each
branch/case. Currently they just pass through the pre-branch state.
